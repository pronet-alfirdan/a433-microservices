version: '2'

services:
  item-app:
    image: pronetalfirdan20/item-app:v1
    restart: "unless-stopped"
    ports:
      - "80:8080"
    depends_on:
      - item-db

  item-db:
    image: mongo:3
    volumes:
      - app-db:/data/db

volumes:
  app-db:
